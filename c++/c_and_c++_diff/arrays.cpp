//
// Created by emukh on 11/27/2025.
//
int main(int argc, char *argv[]) {
    /**
    * sizeOf(A) will return 20 since array A has 5 elements, 4 bytes each.
    * and sizeOf(A[0]) will return 4 since first element is of type int
    * so arraySize will be equal to 5
    */
}
